import 'package:flutter/material.dart';

class BuyCartView extends StatelessWidget {
  const BuyCartView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Buy cart view'),
      ),
    );
  }
}
